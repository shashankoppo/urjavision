services:
  app:
    build: .
    container_name: urja-vision-app
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: always
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/urja.db

