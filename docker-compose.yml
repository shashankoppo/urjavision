version: '3.8'

services:
  app:
    build: .
    container_name: urja-vision-app
    ports:
      - "3000:3000"
    volumes:
      - ./urja.db:/app/urja.db
    restart: always
    environment:
      - NODE_ENV=production
